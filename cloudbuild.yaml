options:
  logging: CLOUD_LOGGING_ONLY
steps:
  # 1. 設定專案與區域
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: bash
    args:
      - '-c'
      - |
        gcloud config set project jon-backend
        gcloud config set compute/zone us-central1-f

  # 2. 打包整個 repository，但排除 .git 目錄，同時忽略 "file changed" 警告
  - name: 'gcr.io/cloud-builders/docker'
    entrypoint: bash
    args:
      - '-c'
      - |
        tar --warning=no-file-changed --exclude='.git' -czf deployment.tar.gz -C /workspace .

  # 3. 傳送 tar 檔到 VM 的 ~/deployment 目錄
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - compute
      - scp
      - 'deployment.tar.gz'
      - 'watermelon0101@jonvm:~/deployment/deployment.tar.gz'
      - '--zone'
      - us-central1-f

  # 4. 在 VM 上解壓 tar 檔，刪除 tarball 並列出目錄內容（或執行其他指令）
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - compute
      - ssh
      - 'watermelon0101@jonvm'
      - '--zone'
      - us-central1-f
      - '--command'
      - |
          mkdir -p ~/deployment &&
          cd ~/deployment &&
          tar xzf deployment.tar.gz &&
          rm deployment.tar.gz &&
          echo "部署完成，檔案結構如下：" &&
          ls -la
